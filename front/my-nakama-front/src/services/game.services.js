/* import api from './config'

const getGames = async () => {
  try {
    const games = await api.get('/users')
    return games.data
  } catch (error) {
    return error.response.data
  }
}

export { getGames }
 */